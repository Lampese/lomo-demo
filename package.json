{
  "name": "lomo-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "mb:build": "moon build --target js",
    "dev": "npm run mb:build && vite",
    "build": "npm run mb:build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
